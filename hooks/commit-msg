#!/bin/sh
# Strip AI agent footers and trailing blank lines from commit messages.
# Rules: .ai/git-commits-guidelines.md

sed -i '' \
    -e '/^Co-[Aa]uthored-[Bb]y:/d' \
    -e '/^Generated with/d' \
    -e '/^🤖/d' \
    "$1"

# Remove trailing blank lines
perl -i -0pe 's/\n+$/\n/' "$1"
