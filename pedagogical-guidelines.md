## Workbook 교수 설계 원칙

Workbook을 만들거나 검토할 때 아래 원칙을 기준으로 따진다.

### 1. Desired Difficulties (Bjork, 1994)

학습에 바람직한 어려움을 의도적으로 넣는다. 쉬운 연습은 착각만 만든다.

| 원칙 | 적용 방법 | 위반 징후 |
|---|---|---|
| **Generation effect** | `???` placeholder를 쓰되, 답의 구조까지 드러내지 않는다 | 빈칸이 한 단어뿐이고 문맥에서 답이 자명함 |
| **Testing effect** | 개념을 가르친 뒤 시간을 두고 인출을 요구한다 | 설명 직후 같은 내용을 표로 채우게 함 (복사 수준) |
| **Interleaving** | 비슷한 개념을 섞어 비교한다 (예: GPT-2 vs GPT-4) | 한 개념만 반복 연습 |
| **Spacing** | 앞 workbook 개념을 뒤 workbook에서 다시 꺼낸다 | 각 workbook이 독립적으로 닫힘 |

**Scaffolding 원칙**: 처음엔 많이 주고 점차 줄인다. 같은 API를 두 번째 쓸 때는 scaffolding을 빼야 한다.

### 2. Flipped Learning

멘토가 먼저 설명하는 패턴(`설명 → 미션`)을 피한다.

**좋은 순서**: 시도 → 실패/의문 → 멘토 설명 → 재시도
**나쁜 순서**: 멘토 설명 → 미션 (따라하기)

| 패턴 | 이름 | 예시 |
|---|---|---|
| 시도 → 에러 → 왜? → 설명 | **Discovery** | `allowed_special` 없이 encode → 에러 → 멘토가 이유 설명 |
| 설명 → 따라하기 | **Recipe** | "이 parameter를 쓰세요" → 그대로 코드 작성 |

Recipe 패턴이 연속 3개 이상이면 재설계한다.

### 3. 통합 미션 (Synthesis)

개별 미션으로 조각을 익힌 뒤, 마무리 전에 **"합쳐서 하나의 프로그램으로 만들어라"** 미션을 넣는다.

**검증 기준**: 수련생이 이 통합 미션을 완료하면, 대응하는 `.py` 파일(예: `c0205.py`)을 참고 없이 작성할 수 있어야 한다.

통합 미션이 없으면 개별 API 호출은 알지만 프로그램을 구성하는 능력은 안 생긴다.

### 4. 능동적 개념 학습

이론 섹션([토론])을 수동 읽기로 두지 않는다.

| 방법 | 예시 |
|---|---|
| 손으로 알고리즘 실행 | "corpus `ab ab cb`에서 BPE merge를 2회 해보세요" |
| 예측 → 확인 | "이 코드의 출력을 먼저 예측하고, 실행해서 맞는지 확인하세요" |
| 비교표 직접 채우기 | 답을 보기 전에 빈 표를 채우게 한 뒤, 미션을 통해 검증 |

### 5. 미션 딱지

개별 미션에 교수법 의도를 드러내는 딱지를 붙인다. 딱지는 단계(stage)가 아니라 유형(type)이다. 한 workbook 안에서 순서 없이 섞여 나올 수 있다.

| 딱지 | 영어명 | 하는 일 | 근거 |
|---|---|---|---|
| **`[채우기]`** | Generation | `???`나 빈 구조만 받고 핵심 코드를 스스로 작성 | Generation Effect (Slamecka & Graf, 1978) |
| **`[해보기]`** | Discovery | 설명 없이 먼저 실행. 에러를 만난 뒤 원인을 찾음 | Productive Failure (Kapur, 2008) |
| **`[맞히기]`** | Prediction | 코드를 실행하기 전에 출력을 예측하고, 실행 결과와 대조 | Prediction Effect (Liew & Howe, 1993) |
| **`[엮기]`** | Synthesis | 앞 미션들을 합쳐 하나의 프로그램을 처음부터 작성. 힌트 없음 | Bloom's Taxonomy — Create 단계 (Anderson & Krathwohl, 2001) |

#### Workbook에서 쓰는 법

기존 `[미션 N]` 뒤에 딱지를 붙인다:

```
### [미션 2-a] [해보기]
아래 코드를 실행해보세요.

### [미션 2-b] [채우기]
에러를 해결한 뒤, ...

### [미션 8-a] [맞히기]
아래 corpus에서 BPE merge를 직접 해보세요.

### [미션 9] [엮기]
아무것도 참고하지 말고, ...
```

#### 딱지 배치 규칙

1. **`[해보기]`는 `[채우기]`보다 먼저**: 설명 전에 시도가 오게 한다 (Flipped Learning)
2. **`[엮기]`는 마무리 직전**: 통합 미션은 workbook 끝에 놓는다
3. **같은 API의 두 번째 등장은 scaffolding을 줄인다**: 첫 번째에 `[채우기]`로 코드 구조를 줬다면, 두 번째에는 `???`만 남기거나 `[엮기]`로 올린다
4. **`[맞히기]`는 [토론] 섹션에 쓰기 좋다**: 이론을 읽기만 하는 대신 예측 exercise로 바꾼다

### 7. Workbook 독립성 (Independence)

Workbook은 정답 코드 파일(예: `c02.py`, `c0205.py`)에 의존해서는 안 된다. 

- **학생의 코드가 기준**: `from c02 import ...` 처럼 외부 정답을 가져오는 대신, 학생이 앞 단계에서 직접 작성한 함수를 복사해 오거나 이어서 쓰도록 유도한다.
- **실체가 되는 코드**: 학생이 워크북을 따라가며 완성한 코드가 그 자체로 완결된 프로그램이 되어야 한다. 정답 파일은 오직 멘토/AI의 내부 검토용 레퍼런스로만 존재해야 한다.

---

### 8. 검토 checklist

Workbook을 만들거나 고칠 때 아래를 확인한다:

- [ ] 통합 미션(`[엮기]`)이 마무리 전에 있는가?
- [ ] `[해보기]`가 최소 1개 있는가?
- [ ] Recipe 패턴(`설명 → [채우기]`)이 3개 연속 나오지 않는가?
- [ ] `???` placeholder가 답 구조를 너무 드러내지 않는가?
- [ ] [토론] 섹션에 능동적 exercise가 있는가?
- [ ] scaffolding이 뒤로 갈수록 줄어드는가?
- [ ] 앞 workbook 개념을 다시 꺼내는 spacing이 있는가?
- [ ] `technical-writing-guidelines.md` 용어 규칙을 지키는가?
